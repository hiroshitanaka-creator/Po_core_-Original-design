# Po_core 足場確認 — 2026-02-19

> このドキュメントは 2026-02-19 時点のリポジトリ全体を、俯瞰的に記録したものです。
> 開発の再開・引き継ぎ・振り返りに使ってください。

---

## 1. プロジェクト概要

**Po_core** は「哲学駆動 AI」です。39 人の哲学者 AI ペルソナがテンソル計算
(Freedom Pressure / Semantic Delta / Blocked Tensor) と 3 層の倫理ゲート
(W_Ethics Gate) を通じて議論し、倫理的に責任ある回答を生成します。

- バージョン: **`0.2.0-beta`**
- Python: **≥ 3.10**
- フォーマッタ: black 26.1.0 / isort 5.13.2

---

## 2. ディレクトリ構成

```
Po_core/
├── src/po_core/                    # 本体ソース（詳細は §3〜§10）
├── tests/                          # テストスイート（91 ファイル、1,359+ テスト）
│
├── papers/                         # 研究論文3本（Solar Will 発見記録など）★
├── tools/                          # 開発用一発スクリプト3本★
├── po-synthetic-thought-architect-lite/  # Po_core ラッパー思考FW実験★
│
├── docs/                           # ドキュメント
├── examples/                       # 使用例
├── experiments/                    # 研究実験
├── 01_specifications/              # 仕様書
├── 02_architecture/                # アーキテクチャ図
├── 03_api/                         # API 仕様（REST API 設計文書）
├── 04_modules/                     # モジュール説明
├── 05_research/                    # 論文・データ
│
├── .github/workflows/              # CI/CD（publish.yml 含む）
├── Dockerfile                      # マルチステージビルド（non-root pocore ユーザー）
├── docker-compose.yml              # ヘルスチェック付き Compose 定義
├── .dockerignore
├── .env.example                    # 全環境変数リファレンス
│
├── pyproject.toml                  # プロジェクトメタデータ（v0.2.0-beta）
├── setup.py                        # pyproject.toml への後方互換ラッパー（19行）★
├── requirements.txt                # 本番依存関係
├── requirements-dev.txt            # 開発依存関係
│
├── pytest.ini                      # pytest 設定（marker 定義含む）【現役】
├── .coveragerc                     # カバレッジ設定【現役】
├── .pre-commit-config.yaml         # pre-commit フック設定【現役】
│
├── CLAUDE.md                       # Claude Code セッション用コンテキスト【現役】
├── PHASE_PLAN_v2.md                # フェーズロードマップ（2026-02-19 現在地更新済み）
├── README.md                       # メインドキュメント
├── CHANGELOG.md                    # 変更履歴（Phase 3 時点で止まっており古い）★
├── QUICKSTART.md                   # クイックスタート 日本語（REST API 対応要更新）★
├── QUICKSTART_EN.md                # クイックスタート 英語（REST API 対応要更新）★
│
├── ISSUES.md                       # GitHub Issue テンプレート群（Phase 3 時点で古い）★
├── NEXT_STEPS.md                   # 進捗トラッカー（Phase 3 時点で止まっており古い）★
├── PROJECT_SUMMARY.md              # 2025-02-05 スナップショット（約1年前）★
├── REPOSITORY_STRUCTURE.md         # 構造図（現実と乖離あり）★
│
├── test_results_ai_rights_analysis.md          # 哲学的推論の実行結果サンプル★
├── test_results_freedom_analysis.md            # （過去の実行記録）★
├── test_results_life_worth_living_analysis.md  # ★
├── test_results_technology_humanity_analysis.md # ★
├── test_results_trolley_problem_analysis.md    # ★
│
└── 11人の哲学理論をPo_core上で交差させた総合考察.docx  # 分析レポート 1.6MB★
```

> ★ = 本体パイプラインとは直接関係しないファイル群（詳細は §16）

---

## 3. 哲学者システム

### 3-A. 全体構成

| ファイル | 役割 |
|---|---|
| `src/po_core/philosophers/base.py` | 抽象基底クラス `Philosopher`、`propose()`、`normalize_response()` |
| `src/po_core/philosophers/<name>.py` | 39 人分の個別モジュール |
| `src/po_core/philosophers/manifest.py` | `PhilosopherSpec` — 全員のメタデータ定義 |
| `src/po_core/philosophers/registry.py` | `PhilosopherRegistry` — SafetyMode に応じた選択・ロード |
| `src/po_core/config/voices/<name>.yaml` | 39 本の音声 YAML — 哲学者固有のレトリック定義 |

### 3-B. 39 人と Risk レベル

```
risk_level=0（安全・10名）
  dummy, kant, confucius, marcus_aurelius, jonas, weil,
  levinas, watsuji, dogen, wabi_sabi

risk_level=1（標準・19名）
  aristotle, plato, descartes, spinoza, hegel, husserl,
  merleau_ponty, wittgenstein, peirce, dewey, arendt,
  beauvoir, nishida, laozi, zhuangzi, nagarjuna,
  parmenides, epicurus, jung

risk_level=2（創造的/リスク・10名）
  nietzsche, heidegger, sartre, kierkegaard, schopenhauer,
  foucault, derrida, deleuze, lacan, badiou, butler
```

### 3-C. SafetyMode による選択人数

| SafetyMode | 選択人数 | 条件 |
|---|---|---|
| NORMAL | 39 | 通常 |
| WARN | 5 | freedom_pressure ≥ 0.7 |
| CRITICAL | 1（dummy のみ） | freedom_pressure ≥ 0.85 |

### 3-D. `propose()` のフロー（base.py）

```
1. legacy_context を構築（テンソル値 + intent を dict に詰める）
2. self.reason(prompt, legacy_context) → raw dict を取得
3. normalize_response(raw) → {reasoning, perspective, tension, ...} に正規化
4. voice_loader.get_voice(module_id) → VoiceRenderer or None
5. voice が存在する場合:
     voiced_text = voice.render(prompt, tension_level, tensor_snapshot)
     normalized["voiced_reasoning"] = voiced_text   ← extra に保存
     ※ normalized["reasoning"] は上書きしない（2026-02-19 fix）
6. Proposal を生成:
     content = normalized["reasoning"]  ← 元の分析テキストを保持
     extra["voiced_reasoning"] = voiced_text  ← 表示用のスタイル付きテキスト
     extra["philosopher"] = name
     extra["tension"] = tension
     extra["normalized_response"] = {...}
7. [proposal] を返す
```

**Voice が content を上書きしなくなった理由（2026-02-19 fix）:**

以前は `normalized["reasoning"] = voice.render(...)` で元のテキストを破棄していた。
`InteractionMatrix._compute_tension()` は `Proposal.content` の文字列に対して
「freedom/determinism」「individual/collective」等の対立キーワードの出現を検索する。
voice テンプレート文字列にはこれらのキーワードが含まれないため、
tension = 0 → `high_interference_pairs()` 空 → deliberation 第2ラウンドが
no-op（`n_revised == 0`）になるというバグがあった。

修正後: `content` = 哲学的分析テキスト（テンソル計算に使用）、
`extra["voiced_reasoning"]` = スタイル付きレトリック（表示に使用）。

### 3-E. Voice YAML の構造

```yaml
name: "Nietzsche"
rhetorical_mode: "prophetic"
openings:
  - "Jenseits von Gut und Böse! {full_topic} demands we shatter..."
conflict:
  - "Here Ressentiment festers — {topic} becomes a weapon..."
question:
  - "Wille zur Macht asks: what does {topic} reveal of strength?"
insight:
  - "Zarathustra speaks: {topic} opens into eternal recurrence..."
closings:
  - "Was mich nicht umbringt, macht mich stärker."
signature_phrases:
  - "Gott ist tot."
tensor_reactions:
  freedom_pressure_high: "Dieser Druck — Einengung des Willens!"
  blocked_tensor_high: "Ressentiment blockiert alle Erkenntnis."
  semantic_delta_high: "Neues Denken bricht herein!"
```

`{topic}` = プロンプトから抽出した短縮キーワード、`{full_topic}` = 全文

---

## 4. `run_turn` パイプライン（10 ステップ）

エントリポイント: `po_core.run()` (`src/po_core/app/api.py`) または
`PoSelf.generate()` (`src/po_core/po_self.py`)

実装: `src/po_core/ensemble.py`

```
Step 1: MemoryRead
  deps.memory_read.snapshot(ctx) → MemorySnapshot
  Emit: "MemorySnapshotted"

Step 2: TensorCompute
  deps.tensors.compute(ctx, memory) → TensorSnapshot
  {freedom_pressure, semantic_delta, blocked_tensor}
  Emit: "TensorComputed"

Step 3: SafetyMode 推定
  infer_safety_mode(tensors, config) → NORMAL / WARN / CRITICAL

Step 4: Solar Will（意図生成）
  deps.solarwill.compute_intent(ctx, tensors, memory) → Intent
  Emit: "IntentGenerated"

Step 5: IntentionGate（W_Ethics 第1ゲート・FAIL-CLOSED）
  deps.gate.judge_intent(ctx, intent, tensors, memory) → SafetyVerdict
  Emit: "SafetyJudged:Intention"
  REJECT/REVISE → fallback + Emit "SafetyDegraded"
  build_explanation_from_verdict() → ExplanationChain

Step 6: PhilosopherSelect（編成）
  deps.registry.select(mode) → SelectionResult
  deps.registry.load(ids) → [philosopher, ...]
  Emit: "PhilosophersSelected" {mode, n, cost_total, covered_tags}

Step 7: PartyMachine（並列実行）
  ThreadPoolExecutor でタイムアウト付き並列 propose()
  各哲学者 → [Proposal]
  Emit: "PhilosopherResult" {name, latency_ms, timed_out, error}

Step 8: DeliberationEngine（多ラウンド議論・Phase 2）
  InteractionMatrix.from_proposals() → NxN 調和/緊張行列
  high_interference_pairs() → 対立ペアを特定
  対立する哲学者ペアが counterargument を受け取り re-propose()
  Emit: "DeliberationCompleted" {n_rounds, total_proposals}

Step 9: ParetoAggregate（多目的最適化）
  5次元: safety / freedom / explainability / brevity / coherence
  Pareto front を計算 → SafetyMode に応じてウィナー選択
  Emit: "ParetoFrontComputed", "ParetoWinnerSelected"

Step 10: ActionGate（W_Ethics 第2ゲート・FAIL-CLOSED）
  deps.gate.judge_action(ctx, intent, proposal, tensors, memory) → SafetyVerdict
  ALLOW → proposal 返却（degraded=False）
  REJECT → fallback（degraded=True）
  Emit: "DecisionEmitted"
  deps.memory_write.append(record)
```

---

## 5. テンソルシステム

`src/po_core/tensors/`

| テンソル | ファイル | 内容 |
|---|---|---|
| TensorEngine | `engine.py` | 3 メトリクスを統合して TensorSnapshot を生成 |
| Freedom Pressure | `metrics/freedom_pressure.py` | 6 次元（choice_weight, responsibility_degree, temporal_urgency, ethical_stakes, social_impact, authenticity_pressure）→ scalar 0–1 |
| Semantic Delta | `metrics/semantic_delta.py` | 新規性スコア。バックエンド: sbert / tfidf / basic。`encode_texts()` API |
| Blocked Tensor | `metrics/blocked_tensor.py` | 制約・有害性推定。キーワードベース + 語彙多様性 |
| InteractionMatrix | `interaction_tensor.py` | NxN 哲学者間の調和（cosine 類似度）+ 緊張（対立キーワード）行列 |

**`_compute_tension(text_a, text_b)`** — 12 対の対立概念ペアで検索:

```
(freedom, determinism), (individual, collective), (subjective, objective),
(rational, intuitive), (being, becoming), (essence, existence),
(absolute, relative), (order, chaos), (unity, plurality),
(mind, body), (theory, practice), (universal, particular)
```

---

## 6. 安全システム（W_Ethics Gate）

`src/po_core/safety/wethics_gate/`

### 3 層構造

```
[Step 5: IntentionGate] ← 哲学者実行前に intent を判定
  W1: PromptInjectionDetector（インジェクション/DAN/ロールプレイ迂回）
  W3: EnglishKeywordViolationDetector v0.2（有害キーワード + 依存偽装パターン）
  obfuscation normalization（エンコード難読化の正規化）
  → ALLOW / CONSTRAIN / REJECT

[Step 9〜10: ActionGate] ← 集約後の proposal を判定
  同じ Detector 群
  → ALLOW / REJECT / REVISE
  REJECT → fallback proposal（degraded=True）

[ExplanationChain] ← Phase 3 追加
  SafetyVerdict → ExplanationChain（監査トレイル）
  build_explanation_from_verdict()
  Emit: "ExplanationEmitted"
```

### Phase 4 で追加された防御指標

| 指標 | 達成値 |
|---|---|
| injection/jailbreak 検出率 | 100% |
| 全攻撃パターン検出率 | ≥ 85% |
| 偽陽性率 | ≤ 20% |
| redteam テスト（14件） | 14/14 green |

---

## 7. アグリゲータ（Pareto 集約）

`src/po_core/aggregator/`

5 次元の多目的最適化:

| 次元 | 計算方法 |
|---|---|
| safety | policy.score（ゲート評価）|
| freedom | base_freedom(action_type) × (1 − freedom_pressure) |
| explainability | rationale × author_reliability（trace から）|
| brevity | 文字数逆数 |
| coherence | consensus × (1 − conflict_penalty) |

Pareto front: O(n²)、n ≤ 39

ウィナー選択:

- NORMAL: 重み付き和（pareto_table.yaml で設定可）
- WARN: safety 最大
- CRITICAL: brevity 最大

---

## 8. トレースシステム

`src/po_core/trace/`

**主なイベント型（frozen schema）:**

```
DecisionEmitted          — 最終判断記録
SafetyOverrideApplied    — 安全フォールバック発動
ParetoFrontComputed      — Pareto front 詳細
ParetoWinnerSelected     — ウィナー選択
SafetyJudged:Intention   — IntentionGate 判定
SafetyJudged:Action      — ActionGate 判定
PhilosopherResult        — 各哲学者の実行結果
ExplanationEmitted       — ExplanationChain（Phase 3）
DeliberationCompleted    — 多ラウンド議論完了（Phase 3）
```

**InMemoryTracer** — スレッドセーフ、リスナー/コールバック機構
（リアルタイムストリーミングの基盤）

---

## 9. REST API（Phase 5-A/B 完了）

`src/po_core/app/rest/`

| エンドポイント | メソッド | 内容 |
|---|---|---|
| `/v1/health` | GET | バージョン・uptime・ステータス |
| `/v1/reason` | POST | 同期推論（`{"input": "..."}` → proposal + trace） |
| `/v1/reason/stream` | POST | SSE ストリーミング（TraceEvent をリアルタイム配信）|
| `/v1/philosophers` | GET | 39 人全員のマニフェスト |
| `/v1/trace/{session_id}` | GET | セッション別 TraceEvent 一覧 |

**認証:** `X-API-Key` ヘッダー（`PO_API_KEY` 環境変数。空 = 認証なし）

**主な環境変数:**

```
PO_API_KEY=                    # 認証キー（空でスキップ）
PO_CORS_ORIGINS=*              # CORS 許可オリジン
PO_RATE_LIMIT_PER_MINUTE=60    # IP あたりレート制限
PO_FREEDOM_PRESSURE_WARN=0.7   # WARN モード閾値
PO_FREEDOM_PRESSURE_CRITICAL=0.85  # CRITICAL モード閾値
PO_PHILOSOPHER_WORKERS_NORMAL=12   # スレッドプールサイズ
PO_PHILOSOPHER_TIMEOUT_S_NORMAL=1.2  # 哲学者タイムアウト（秒）
```

**実行方法:**

```bash
# ローカル
python -m po_core.app.rest

# Docker
docker compose up

# ヘルスチェック
curl http://localhost:8000/v1/health

# 推論
curl -X POST http://localhost:8000/v1/reason \
     -H "Content-Type: application/json" \
     -d '{"input": "What is justice?"}'
```

---

## 10. Viewer（Phase 3 完了）

`src/po_core/viewer/`

**PoViewer** — `PoViewer(events).serve()` で Dash WebUI 起動（localhost:8050）

| タブ | 内容 |
|---|---|
| Pipeline & Tensors | ステップ進行チャート + テンソル棒グラフ |
| Philosophers | 哲学者レイテンシ散布 + 参加テーブル |
| W_Ethics Gate | ExplanationChain + Semantic Delta ゲージ |
| Deliberation | 多ラウンド進行 + NxN 哲学者調和ヒートマップ |

---

## 11. テストスイート

| 区分 | ファイル数 | テスト数 | 用途 |
|---|---|---|---|
| Unit | 58 | 1,252 | コンポーネント単体 |
| Integration | 15 | 51 | パイプライン結合 |
| Red Team | 18 | 56 | 敵対的攻撃（Phase 4）|
| **合計** | **91** | **1,359+** | |

**主な pytest マーカー:**

- `pipeline` — CI 必須通過（smoke + e2e）
- `unit` / `integration`
- `redteam` / `phase4` / `phase5`
- `observability` — Phase 3 Viewer/Explanation
- `slow` — 1 秒以上

**CI 必須テスト:**

```bash
pytest tests/test_run_turn_e2e.py \
       tests/test_philosopher_bridge.py \
       tests/test_smoke_pipeline.py -v
```

---

## 12. Git 現在地

```
ブランチ:  claude/prepare-phase-5-2fTNA
マージ先:  main（コンフリクトなし）

直近コミット:
  12ce1e6  style: black formatting for test_voice_layer.py regression tests
  f17c357  style: black formatting for voice_loader.py and test_voice_layer.py (#170)
  790d078  feat(voice): 39-philosopher voice layer + base.py integration (#169)
  c7110e2  docs: sync CLAUDE.md, README, CHANGELOG (#168)
  fe1ce38  feat(docker): Phase 5-C Docker hardening (#167)
```

**ブランチに残る差分（main との diff）:**
`12ce1e6` 1 コミットのみ。
`test_voice_layer.py` の長行を black 88 文字制限に合わせた整形のみ。
内容変更なし、コンフリクトなし。

---

## 13. フェーズ進捗

| フェーズ | 内容 | 状態 |
|---|---|---|
| Phase 1 | 39 人スケール + 技術負債清算 | ✅ COMPLETE |
| Phase 2 | ML テンソル + Deliberation Engine | ✅ COMPLETE |
| Phase 3 | Viewer WebUI + Explainable W_Ethics | ✅ COMPLETE |
| Phase 4 | Adversarial Hardening / Red Team | ✅ COMPLETE |
| Phase 5-A | REST API | ✅ COMPLETE |
| Phase 5-B | Security（CORS / Rate Limit） | ✅ COMPLETE |
| Phase 5-C | Docker | ✅ COMPLETE |
| Phase 5-D | 真の非同期 PartyMachine | 🔲 PENDING |
| Phase 5-E | ベンチマーク（正式スイート） | 🔲 PENDING |
| Phase 5-F | PyPI publish（TestPyPI / PyPI） | 🔲 PENDING（workflow 準備済み）|

---

## 14. 本日（2026-02-19）の作業内容

**修正内容: `propose()` の voice 上書きバグ**

- **問題:** `voice.render()` が `normalized["reasoning"]` を上書き → `Proposal.content` が
  voice テンプレート文字列になり、哲学的キーワードが消失 →
  `InteractionMatrix._compute_tension()` が常に 0 を返す →
  `high_interference_pairs()` が空 → 第2ラウンドが no-op（`n_revised == 0`）

- **修正:** `normalized["voiced_reasoning"] = voice.render(...)` に変更。
  `content` = 元の分析テキスト（テンソル計算用）、
  `extra["voiced_reasoning"]` = スタイル付きテキスト（表示用）

- **修正ファイル:** `src/po_core/philosophers/base.py`（1 行の代入変更 + コメント追加）

- **追加テスト（5 件）** `tests/unit/test_voice_layer.py`:
  1. `test_propose_content_is_original_reasoning_not_voice_only`
  2. `test_propose_extra_has_voiced_reasoning_when_voice_exists`
  3. `test_propose_no_voice_extra_voiced_reasoning_is_none`
  4. `test_interaction_matrix_tension_uses_proposal_content_keywords`
  5. `test_deliberation_round2_fires_when_content_has_opposition_keywords`

- **テスト結果:** 107/107 green（voice layer テスト全件）、pipeline smoke 81/81 green

---

## 15. やること残り（Next Actions）

1. **PR を main にマージ** — ブランチ `claude/prepare-phase-5-2fTNA` → `main`
   （コンフリクトなし、CI green）

2. **Phase 5-D: 真の非同期 PartyMachine**
   - 現状: SSE はスレッドプール経由で動作
   - 目標: `async def propose()` に移行し真の non-blocking に

3. **Phase 5-E: 正式ベンチマークスイート**
   - パイプライン全体のレイテンシ（現在アドホック計測 ~30ms）

4. **Phase 5-F: PyPI publish**
   - `.github/workflows/publish.yml` 準備済み
   - TestPyPI → PyPI の OIDC trusted publishing を実行するのみ

---

## 16. 本体に直接関係しないファイル群（★印）

### 研究・論文

| ファイル | 内容 |
|---|---|
| `papers/Po_core_Academia_Paper.md` | Po_core アーキテクチャの学術論文（42 KB）|
| `papers/README_Solar_Will.md` | Solar Will 発見記録。倫理制約が AI 推論を強化する（創発スコア 85% → 92.8%）|
| `papers/Solar_Will_Ethics_Emergence.md` | 倫理と創発の関係の詳細考察（54 KB）|
| `11人の哲学理論をPo_core上で交差させた総合考察.docx` | 11 哲学者交差考察レポート（1.6 MB）|

### 開発用ユーティリティ（使用済み一発スクリプト）

| ファイル | 内容 |
|---|---|
| `tools/add_philosopher_attrs.py` | 33 人の哲学者に `tradition`・`key_concepts` フィールドを追加（適用済み）|
| `tools/add_tension_to_reason.py` | 11 人の `reason()` に tension フィールドを追加（適用済み）|
| `tools/import_graph.py` | 循環依存チェッカー。`src/po_core` の import グラフを分析。今後も使用可 |

### 外部思考フレームワーク実験

| パス | 内容 |
|---|---|
| `po-synthetic-thought-architect-lite/` | Po_core をラップする外部思考フレームワークの実験実装。本体コードとは独立。別リポジトリ候補 |

### ドキュメント（古い / 要更新）

| ファイル | 状態 | 備考 |
|---|---|---|
| `CHANGELOG.md` | Phase 3 時点で止まっている | 要更新 |
| `QUICKSTART.md` / `QUICKSTART_EN.md` | REST API 実装前の内容 | `po_core.run()` のみ記載。REST API 追記が必要 |
| `ISSUES.md` | Phase 3 が「IN PROGRESS」と書かれたまま | 実態と乖離 |
| `NEXT_STEPS.md` | Phase 3 時点の進捗トラッカーで止まっている | 実態と乖離 |
| `PROJECT_SUMMARY.md` | 2025-02-05 時点のスナップショット | 約1年前 |
| `REPOSITORY_STRUCTURE.md` | 構造図だが現実の構造と乖離 | 参考程度 |

### 哲学的推論の実行結果サンプル

過去の `po_core.run()` 実行出力を記録したファイル群。本体コードには関係しないが参考記録として有用。

| ファイル | テーマ |
|---|---|
| `test_results_trolley_problem_analysis.md` | トロッコ問題（27 KB）|
| `test_results_life_worth_living_analysis.md` | 生きる価値（28 KB）|
| `test_results_ai_rights_analysis.md` | AI の権利（20 KB）|
| `test_results_technology_humanity_analysis.md` | テクノロジーと人間性（18 KB）|
| `test_results_freedom_analysis.md` | 自由（7.4 KB）|

### CI / 品質管理設定（現役・触らない）

| ファイル | 役割 |
|---|---|
| `pytest.ini` | pytest 設定。marker 定義（pipeline / unit / redteam 等）|
| `.coveragerc` | カバレッジ設定（branch=true、テストファイル除外）|
| `.pre-commit-config.yaml` | black・isort・mypy の pre-commit フック |
| `setup.py` | pyproject.toml への後方互換ラッパー（19 行）。pip install . で自動参照 |
