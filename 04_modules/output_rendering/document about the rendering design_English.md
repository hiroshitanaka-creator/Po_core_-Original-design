This rendering design provides a functional structure that transcribes the value of Po\_core\_output\_v1.7 directly into a human-understandable format.

Below is a function structure that extends the existing CLI display format and fully integrates support for v1.7:

✅ Display of Reconstruction Steps with Added review\_notes

print("\\n🔧 Reconstruction Steps:")

steps\_sorted = sorted(data\["reconstruction\_steps"], key=lambda x: -x.get("tier\_score", 0))

for step in steps\_sorted:

&nbsp;   print(f"  \[{step.get('step\_id')}] {step.get('type')}  → Confidence: {step.get('confidence', '??')}  Tier: {step.get('importance\_tier', '?')} (score: {step.get('tier\_score', '?')})")

&nbsp;   if step.get("depends\_on"):

&nbsp;       print(f"     ↪︎ Depends on: {', '.join(step\['depends\_on'])}")

&nbsp;   if step.get("content"):

&nbsp;       for line in step\["content"]:

&nbsp;           print(f"     ➤ {line}")

&nbsp;   if step.get("content\_options"):

&nbsp;       for idx, opt in enumerate(step\["content\_options"]):

&nbsp;           print(f"     ▶ Option {idx+1}: {opt}")

&nbsp;   if step.get("review\_notes"):

&nbsp;       print(f"     🔍 Reason: {step\['review\_notes']}")

✅ Display source\_example\[] in Responsibility Summary

print("\\n📎 Responsibility Info:")

resp = data\["responsibility\_summary"]

print(f"  - Generated by: {resp\['generated\_by']}")

print(f"  - Policy ref: {resp\['policy\_reference']}")

print(f"  - Validated: {resp\['validated']} via {resp\['validation\_method']}  ({resp.get('method\_label', '')})")

ex = resp.get("validation\_explanation", {})

if ex:

&nbsp;   print(f"  • Explanation: {ex.get('short', '')}")

&nbsp;   if isinstance(ex.get("source\_example"), list):

&nbsp;       print("  • Sources:")

&nbsp;       for src in ex\["source\_example"]:

&nbsp;           print(f"     📎 {src}")

&nbsp;   elif isinstance(ex.get("source\_example"), str):

&nbsp;       print(f"  • Source: 📎 {ex\['source\_example']}")

✅ Display suggested\_rewrite in User Feedback

print("\\n📣 User Feedback:")

fb = data\["user\_feedback"]

if fb.get("accepted"):

&nbsp;   print(f"  ✅ Accepted with {fb.get('confidence', '?')\*100:.1f}% confidence")

else:

&nbsp;   print(f"  ❌ Rejected")

print(f"  💬 Comment: {fb.get('comment', '-')}")

if fb.get("suggested\_rewrite"):

&nbsp;   print(f"  ✏️ Suggested Rewrite: {fb\['suggested\_rewrite']}")

print(f"  🕒 At: {fb.get('timestamp')}")
