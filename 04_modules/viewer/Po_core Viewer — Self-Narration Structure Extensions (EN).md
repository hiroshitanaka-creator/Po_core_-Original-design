# **Po_core Viewer — Self-Narration Structure Extensions**

## **1. Overview**

Three extension modules evolve the Viewer into a structure that narrates the crossing history of meaning evolution and human interventions.

## **2. Modules**

| Module | Content | Purpose |
| :---- | :---- | :---- |
| 🟡 `decay_heat_overlay` | Fuse `reason_confidence × decay_factor × priority_score` into a heatmap | Visualize freshness and pressure density; reveal reconstructable zones |
| 🔵 `path-highlight-mode` | One-stroke highlighting of `contextual_influence_path[]` | Let users “walk” the path of narrative redirection |
| 📘 `semantic-evolution-journal` | Summarize axis changes and reason distributions in natural language | Make Po_core generate its own evolution context |

## **3. Details**

- `decay_heat_overlay`: red = high refire pressure; blue = past intervention.
- `path-highlight-mode`: click a step to highlight its causal path.
- `semantic-evolution-journal`: e.g., “Emotion axis decreased; factual axis strengthened.”

## **4. Outlook**

The Viewer reframes logs as a *story of growth*—semantic, ethical, and resonant.
